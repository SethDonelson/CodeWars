//take in a number, turn to a string
function numberToString(num) {
    return num.toString()
   }


   
   //return *50 +6 if a number, else return error

   function problem(x){
    if(typeof x !== 'number'){ 
      return "Error"} 
    else { 
      return (x * 50 + 6)
    }
  }
  
  //given a number, positive, integer or num
  //return x50 + 6, or erro
  // 1  = 56
  // if(x != number){ return "Error"} else{ return ((x *50) +6)}
  